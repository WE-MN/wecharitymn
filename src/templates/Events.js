import React from 'react';
import NavigationBar from './NavigationBar';


const Events = (props) => {
    return(
        <div>
            <NavigationBar/>
        </div>
        
    );
}

export default Events;